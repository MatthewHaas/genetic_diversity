# 13 February 2020
# Steps for installing polyRAD (https://github.com/lvclark/polyRAD)

# Some Bioconductor packages are required.
if (!requireNamespace("BiocManager", quietly = TRUE)){
  install.packages("BiocManager")
}
BiocManager::install("pcaMethods")

# This package is required to import from VCF.
BiocManager::install("VariantAnnotation")
# Install polyRAD
install.packages("polyRAD")

# I am now able to load polyRAD